
package user;

import java.util.HashSet;
import java.util.Set;
import airline.flight.*;

import static util.Assertions.*;


/**
 * This is a user class
 * 
 * @author Li Ju
 */

public abstract class User{
	

	/**
	 * searches available flights by entering a departure date, and travel origin and destination.
	 * @param departureDate 
	 * 					the desired departure date
	 * @param origin
	 * 					the desired original location of the flight
	 * @param destination
	 * 					the desired destination of the flight
	 * @return Set<FlightNode>
	 * 					return a set of FlightNode which satisfied all three requirements
	 */
	
	@SuppressWarnings("unused")
	public static Set<FlightNode> searchFlights (String departureDate, String origin, String destination) {
		checkNotNull(departureDate);
		checkNotNull(origin);
		checkNotNull(destination);
		Set<FlightNode> flightSatisfiedDepartureDate = new HashSet<FlightNode>();
		
		
		
		
		return null;
		
	}
	
	/**
	 * searches available itineraries by entering a departure date, and travel origin and destination.
	 * @param departureDate 
	 * 					the desired departure date
	 * @param origin
	 * 					the desired original location of the flight
	 * @param destination
	 * 					the desired destination of the flight
	 */
	
	public static void searchItineraries (String departureDate, String origin, String destination) {
		checkNotNull(departureDate);
		checkNotNull(origin);
		checkNotNull(destination);
	}
	
	/**
	 * books the itinerary by entering the itinerary's ID
	 * @param itineraryID
	 * 				the ID of the itinerary
	 */
	public static void bookItineraries (int itineraryID) {
		
	}
	
	public static void uploadClientInfo(String path) {
		//TODO
	}
	
	public static void uploadFlightInfo(String path) {
		//TODO
	}
}
	
